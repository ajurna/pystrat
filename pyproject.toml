[project]
name = "pystrat"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "pillow",
    "pyinstaller>=6.18.0",
    "svglib>=1.6.0",
    "reportlab>=4.4.4",
]

[project.scripts]
build-exe = "build_exe:main"
